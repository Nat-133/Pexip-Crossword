Wordsearch solver hashes substrings in the grid up to a certain length, and then matches those with the word given in is_present

Each python file can be run to complete some tests, or can be imported to use the methods individually.